html
    head
        title DIS_MISURA
        meta(name="viewport" content="width=device-width, initial-scale=1 shrink-to-fit=no")
        meta(name="theme-color" content="#000")
        link(rel="stylesheet" href="/style.css")
        script(src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/2.2.0/socket.io.js", type="text/javascript")
        script(src="https://code.jquery.com/jquery-3.3.1.min.js" integrity="sha256-FgpCb/KJQlLNfOu91ta32o/NMZxltwRo8QtmkMRdAu8=" crossorigin="anonymous")
    body
        #random-container        

        #main-container
            | Ogni cosa che facciamo e la realtà intorno a noi è calcolabile e misurabile. 
            br
            | Possiamo raccogliere una quantità infinita di dati. 
            br
            | Che cosa accadrebbe se tutte queste informazioni diventassero visibili nella realtà? 
            br
            | Vieni a scoprirlo con Dismisura 
            br
            br
            | è tutto sotto controllo.
        
        #banner-container
            #scroll-icon
                img(src="down.svg" onclick="handleScroll({deltaY: 1})")
            #banner
                img#logo(src="/logo.svg")
                span BASE Milano 18–19 marzo 2019

        script.
            totalConnections="#{totalConnections}"
            dailyConnections="#{dailyConnections}"
            totalClicks="#{totalClicks}"
            dailyClicks="#{dailyClicks}"
            totalTime="#{totalTime}"
        script(src="/app.js", type="text/javascript")